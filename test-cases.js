const classes = require("./classes"); // Allows for the use of classes from classes.js


// Creation of rase information class

const Dragonborn = new classes.raceClass('Dragonborn', 2, 0, 0, 0, 0, 1, 'no');
console.log(Dragonborn.twobyany);